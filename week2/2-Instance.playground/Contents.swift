/**
 인스턴스
 - 인스턴스는  class, struct, enum과 같은 설계도를 기반으로 실제 메모리에 생성되는 실체
 - class, struct, enum은 설계도의 역할을 함
 - 인스턴스는 설계도를 보고 직접 만들어져서 메모리에 저장됨

 인스턴스의 init
 - 인스턴스를 생성할 때 상태를 초기화해야 함
 - 변수, 상수(프로퍼티)의 값을 정해주어야 함
 */
